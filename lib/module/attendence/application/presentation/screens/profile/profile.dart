import 'package:flutter/material.dart';

class ScreenAtendenceProfile extends StatelessWidget {
  const ScreenAtendenceProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('AtendenceProfileScreen'),
      ),
    );
  }
}
