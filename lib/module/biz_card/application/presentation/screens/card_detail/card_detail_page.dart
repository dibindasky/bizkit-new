import 'package:flutter/material.dart';

class BizCardDetailScreen extends StatelessWidget {
  const BizCardDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}