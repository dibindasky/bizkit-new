import 'package:flutter/material.dart';

class AttendenceMSLeaveAndRequestsScreen extends StatelessWidget {
  const AttendenceMSLeaveAndRequestsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('AttendenceMS Leave And Requests Screen'),
      ),
    );
  }
}
