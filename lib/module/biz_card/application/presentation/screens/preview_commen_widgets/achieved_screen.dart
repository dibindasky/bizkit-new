import 'package:flutter/material.dart';

class AchievedScreen extends StatelessWidget {
  const AchievedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('AchievedScreen'),
      ),
    );
  }
}
