import 'package:flutter/material.dart';

class ScreenAtendenceHome extends StatelessWidget {
  const ScreenAtendenceHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('AtendenceHomeScreen'),
      ),
    );
  }
}
