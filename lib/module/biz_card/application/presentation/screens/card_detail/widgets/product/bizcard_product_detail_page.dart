import 'package:flutter/material.dart';

class BizcardProductDetailScreen extends StatelessWidget {
  const BizcardProductDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Scaffold(
        appBar: AppBar(),
        body: SafeArea(
          child: Column(
            children: [Text('asdfasdf')],
          ),
        ),
      ),
    );
  }
}
