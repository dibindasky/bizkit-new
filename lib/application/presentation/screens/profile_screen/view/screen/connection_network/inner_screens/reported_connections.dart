import 'package:flutter/material.dart';

class ReportedConnections extends StatelessWidget {
  const ReportedConnections({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
