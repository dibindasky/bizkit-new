import 'package:flutter/material.dart';

class AddParticipentPopUp extends StatelessWidget {
  const AddParticipentPopUp({super.key});

  @override
  Widget build(BuildContext context) {
    return BottomSheet(
      onClosing: () {},
      builder: (context) => Container(),
    );
  }
}
