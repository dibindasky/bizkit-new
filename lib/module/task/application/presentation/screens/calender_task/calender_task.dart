import 'package:flutter/material.dart';

class ScreenCalenderTaskView extends StatelessWidget {
  const ScreenCalenderTaskView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ScreenCalenderTaskView'),
      ),
    );
  }
}
