import 'package:flutter/material.dart';

class AttendenceTeamTimeSheetScreen extends StatelessWidget {
  const AttendenceTeamTimeSheetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: SafeArea(
        child: Center(child: Text('Attendence Team TimeSheet Screen')),
      ),
    );
  }
}
