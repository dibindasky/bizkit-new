import 'package:flutter/material.dart';

class ScreenTaskProfile extends StatelessWidget {
  const ScreenTaskProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Profile task'),
      ),
    );
  }
}
