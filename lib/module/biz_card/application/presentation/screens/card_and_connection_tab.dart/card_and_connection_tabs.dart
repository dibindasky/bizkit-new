import 'package:flutter/material.dart';

class BizCardAndConnectionScreen extends StatelessWidget {
  const BizCardAndConnectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}