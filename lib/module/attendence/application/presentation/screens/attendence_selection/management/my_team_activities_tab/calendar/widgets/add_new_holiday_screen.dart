import 'package:flutter/material.dart';

class AttendenceMsAddNewHolidayScreen extends StatelessWidget {
  const AttendenceMsAddNewHolidayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
